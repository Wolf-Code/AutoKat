import { View } from '../../router/View'
import { Link } from '../components/Link'

export const WiFi : View = {
	render: () => {
		return `<div>
			wifi page
		</div>`
	},
	preRender: () => {

	},
	postRender: () => {

	}
}